export interface DataTableParticipantesItem {
    // Nombre completo.
    // Universidad de Procedencia.
    // Identificación.
    // Carné estudiantil.
    // Fecha de nacimiento.
    // Correo electrónico.
  
      email: string;
      firstName: string;
      lastName: string;
      occupation: string;
      phoneNumber: string;
      title: string;
      x: string;
  }
  export var displayedColumns = ['email', 'firstName','lastName','occupation','phoneNumber','title','x'];